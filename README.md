# resnet18-transfer-learning-cifar10-pytorch
Transfer learning using a pretrained ResNet18 model on the CIFAR-10 dataset with PyTorch. The pretrained backbone is frozen, and only the final classification layer is trained.
